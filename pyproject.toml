[project]
name = "grading-system"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pymupdf",
    "pytesseract",
    "pillow",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
]